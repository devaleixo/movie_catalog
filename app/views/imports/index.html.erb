<div class="text-center mb-5">
  <h1>📋 Histórico de Importações</h1>
  <p class="lead" style="color: var(--color-gold);">Acompanhe o status das suas importações de filmes</p>
</div>

<div class="mb-4 text-end">
  <%= link_to '➕ Nova Importação', new_import_path, class: 'btn btn-primary' %>
</div>

<% if @import_statuses.any? %>
  <div class="row g-3">
    <% @import_statuses.each do |import_status| %>
      <div class="col-12">
        <div class="card" style="background-color: var(--color-dark-gray); border: 1px solid var(--color-medium-gray); border-radius: 12px;">
          <div class="card-body">
            <div class="row align-items-center">
              <div class="col-md-6">
                <h5 class="card-title mb-2" style="color: var(--color-gold);">
                  📄 <%= import_status.filename %>
                </h5>
                <p class="text-muted mb-1">
                  <small>Criado em: <%= import_status.created_at.strftime('%d/%m/%Y às %H:%M') %></small>
                </p>
              </div>
              
              <div class="col-md-3 text-center">
                <% case import_status.status %>
                <% when 'pending' %>
                  <span class="badge bg-secondary" style="font-size: 1rem; padding: 0.5rem 1rem;">
                    ⏳ Pendente
                  </span>
                <% when 'processing' %>
                  <span class="badge bg-info" style="font-size: 1rem; padding: 0.5rem 1rem;">
                    ⚙️ Processando
                  </span>
                <% when 'completed' %>
                  <span class="badge bg-success" style="font-size: 1rem; padding: 0.5rem 1rem;">
                    ✅ Concluído
                  </span>
                <% when 'failed' %>
                  <span class="badge bg-danger" style="font-size: 1rem; padding: 0.5rem 1rem;">
                    ❌ Falhou
                  </span>
                <% end %>
                
                <% if import_status.completed? %>
                  <div class="mt-2">
                    <small class="text-muted">
                      <%= import_status.success_count %> sucesso | 
                      <%= import_status.error_count %> erros
                    </small>
                  </div>
                <% elsif import_status.processing? && import_status.total_rows %>
                  <div class="mt-2">
                    <small class="text-muted">
                      <%= import_status.processed_rows %> / <%= import_status.total_rows %>
                    </small>
                  </div>
                <% end %>
              </div>
              
              <div class="col-md-3 text-end">
                <%= link_to '👁️ Ver Detalhes', import_path(import_status), class: 'btn btn-secondary btn-sm' %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="d-flex justify-content-center mt-4">
    <%= paginate @import_statuses %>
  </div>
<% else %>
  <div class="text-center py-5">
    <div class="card" style="background-color: var(--color-dark-gray); border: 1px solid var(--color-medium-gray); padding: 3rem;">
      <h3 style="color: var(--color-light-gray);">Nenhuma importação encontrada</h3>
      <p class="text-muted mb-4">Você ainda não fez nenhuma importação em massa de filmes.</p>
      <%= link_to '📂 Fazer Primeira Importação', new_import_path, class: 'btn btn-primary btn-lg' %>
    </div>
  </div>
<% end %>

<div class="mt-4 text-center">
  <%= link_to '⬅ Voltar para Filmes', movies_path, class: 'btn btn-secondary' %>
</div>
